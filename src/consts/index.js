const STATUS = require('./status');
const VERSION = require('./version');
const BUILDINGS = require('./building');
const AVERAGE_IDS = require('./averagesIds');

module.exports = {
  STATUS,
  VERSION,
  BUILDINGS,
  AVERAGE_IDS,
};
